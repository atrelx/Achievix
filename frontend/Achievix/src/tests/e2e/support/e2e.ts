// frontend/Achievix/src/tests/e2e/support/e2e.ts

// This is a great place to put global configuration and
// behavior that modifies Cypress.
//
// You can read more here:
// https://on.cypress.io/configuration

// Import commands.js using ES2015 syntax:
// import './commands' // if you create a commands.ts file in this directory

// You can also add global before/after hooks here:
// beforeEach(() => {
//   // e.g., cy.log('Global beforeEach hook')
// });

// To handle uncaught exceptions from your application:
// Cypress.on('uncaught:exception', (err, runnable) => {
//   // returning false here prevents Cypress from
//   // failing the test
//   return false
// })
